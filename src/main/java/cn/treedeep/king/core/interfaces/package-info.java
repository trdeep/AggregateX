/**
 * Interfaces（接口层）
 * <p>
 * 更广义的“系统对外暴露的交互方式”（包含 Presentation 和其他外部协议）
 * <p>
 * 更广义的“系统对外暴露的交互方式”（包含 Presentation 和其他外部协议）
 * 不仅包括用户交互（Presentation），还包括与其他系统（如 RPC、消息队列）的交互
 */
package cn.treedeep.king.core.interfaces;
